apiVersion: apps/v1
kind: Deployment
metadata:
  name: listener
spec:
  template:
    spec:
      containers:
        - name: listener
          securityContext:
            capabilities:
              drop:
                - ALL
              add:
                - NET_BIND_SERVICE
